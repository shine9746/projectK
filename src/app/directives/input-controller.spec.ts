import { InputController } from './input-controller';

describe('InputController', () => {
  it('should create an instance', () => {
    const directive = new InputController();
    expect(directive).toBeTruthy();
  });
});
